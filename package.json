{
  "name": "docs-developer-blog",
  "private": true,
  "scripts": {
    "make-install": "pip3 install -r requirements.txt && make install",
    "start": "make serve",
    "serve": "make hugo-serve",
    "build": "pip3 install -r requirements.txt && make install && hugo && npm run build:functions",
    "build:functions": "netlify-lambda build assets/lambda",
    "server": "hugo server"
  },
  "dependencies": {
    "atomic-algolia": "^0.3.17",
    "autoprefixer": "^9.7.6",
    "netlify-lambda": "^1.6.3",
    "postcss-cli": "^7.1.1"
  },
  "browserlists": [
    "ie >= 8",
    "last 3 versions"
  ]
}
